// It is used to concat multiple strings;
// It is preferred to use + or += for concatenating strings;

const str = "hello world ";
const str1 = "Javascript is great";

const res = str.concat(str1);
console.log(res); // hello world Javascript is great
