// This method is used to remove element from the end of an array and return the removed element;
// It modifies the original array;

const arr = [1, 2, 3, 4, , 6];
const res = arr.pop();
const res1 = arr.pop();
console.log(res);
console.log(res1);

//if element is empty then it returns undefined;
